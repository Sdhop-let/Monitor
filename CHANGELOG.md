# 更新日志 (CHANGELOG)

> 本文档记录 `Monitor · API 额度监控` 的所有变更、优化与修复。

---

## v1.2.0 — 新增阶跃星辰 StepFun，支持自定义查询

> 本次发布把这套监控从「内置平台的固定名单」扩展成**可自己接线**的工具：
> 新增 StepFun 官方接口通道，并允许你为任意站点自填接口地址与解析规则。
> 同时引入**按账户计价币种**，人民币站与美元站终于能各按各的口径告警。

### ✨ 新增功能

#### 1. 阶跃星辰 StepFun 平台接入

| 能力 | 实现 |
|---|---|
| 余额 | `GET https://api.stepfun.com/v1/accounts`（Bearer `sk-`，**CNY**） |
| 赠送额度 | `total_voucher_balance` → 复用「赠」通道与额度卡片 |
| 模型列表 | `GET /v1/models` |

- 接口**无需额外凭据**，填 API Key 即可
- 币种常量表登记为 **CNY**（阈值默认 ¥10）
- 卡片显示 `¥120.50（现金 100.00 / 赠券 20.50）`，**备注只列 >0 的项** ——
  0.00 的含义是「没有这项」而不是「有 0 块」，列出来只会误导

#### 2. 用户自定义查询（自己接线任意站点）

新增 `自定义查询` 平台类型，提供**三种解析器**，按站点能力自选：

| 模式 | 行为 | 适用场景 |
|---|---|---|
| **OpenAI / One API 兼容** | 复用 New API 双通道（`/api/user/self` → billing） | 标准中转站 |
| **JSON 字段路径** | 取 JSON 字段，**支持数组下标**（`data.balance`、`balance_infos.0.total_balance`） | 绝大多数自建服务 |
| **正则表达式提取** | 在响应文本里跑正则，有捕获组取第 1 组 | 返回网页 / 非 JSON 的站点 |

- 可另填「赠送额度字段路径」→ 自定义站也能进「赠」通道，复用额度卡片并参与临期提醒
- 只填**余额接口地址**即可，站点根地址会**自动推导**（`https://api.x.com/v1/balance` → `https://api.x.com`）
- 保存时自动保护协议头，不会把 `https://` 截断成 `https:`

#### 3. 按账户计价币种

此前所有阈值都按人民币理解，美元站只能自己心算。现在：

- 内置平台币种写死常量表：**DeepSeek / Kimi / 智谱 / StepFun = CNY，New API / OpenRouter = USD**
- **只有 New API 与自定义查询**允许用户覆盖币种（站点既可能收人民币也可能收美元）
- 阈值默认值随币种走：**人民币站 ¥10 / 美元站 $1**
- 通知文案按币种补符号，不再出现 `$` 站显示 `¥` 的错位

### 🔧 优化与修复

#### 4. 修复 `baseUrl` 被截断成 `https:`（真机验证发现）

- **问题**：保存 Base URL 时 `trimEnd('/')` 把协议头的 `//` 一起砍掉，
  存成 `https:` → 自定义查询的兼容模式与模型列表探测会打出 `https:/v1/accounts` 畸形 URL。
- **影响面**：只有畸形输入的账户会中招，但**界面看不出任何异常**，只在请求层静默失败。
- **修复三处**：保存时保护协议头 + 从余额地址反推站点根 + 网络层加残缺协议兜底与空主机人话报错。

#### 5. 修复 StepFun / 自定义站的赠送额度显示不出来

- **问题**：赠送额度块的判据写死 `type == ZHIPU`，导致 StepFun 与自定义站**算出来了却渲染不出来**。
- **修复**：判据改为「有没有额度数据」而非「是哪家平台」—— 这本就该是能力判断，不是平台判断。

#### 6. 其他
- 模型列表请求抽出统一实现，Kimi / StepFun / 自定义三家共用（去重复代码）
- 账户去重键纳入**解析规则**：同站同 Key 但解析方式不同不再被误判为重复账户而互相覆盖
- 编辑页按平台能力动态显示字段：智谱无「系统访问令牌」、StepFun 地址只读、自定义才有「解析规则」区

### ✅ 真机验证

在 **OnePlus PLK110（Android 16）** 上完成 15 项实机验证，全程无崩溃：

- StepFun 走真实网络请求返回 `HTTP 401`（假 Key）→ 反证 URL 拼接、鉴权头、路径全部正确
- 自定义 JSON 路径模式成功读到 `¥59`，赠送额度 `¥60.00` 正常上卡
- 错误链路完整：状态「异常」+ 错误码 + 备注三层齐全
- 编辑页回显与覆盖安装数据保留均正常（向后兼容无需迁移）

> 说明：StepFun 的**成功**路径需真实 API Key 才能验，本次仅验证了鉴权失败路径；自定义查询的「兼容模式」与「正则模式」同理，待有对应站点时补充验证。

### 📦 下载信息

- **版本号**：1.2.0
- **版本代码**：3
- **最低系统**：Android 8.0 (API 26)
- **目标系统**：Android 15 (API 35)
- **编译版本**：Android 16 (API 36，灵动岛需要)
- **构建工具**：AGP 8.9.1 / Gradle 8.14.4 / Kotlin 2.0.21

---

## v1.1.0 — 余额告警接入灵动岛

> 本次发布聚焦**通知体系**与**刷新策略**两处长期痛点，并修正了若干会导致重复打扰或多余请求的问题。

### ✨ 新增功能

#### 1. 余额告警接入 Android 16 / 17 灵动岛（Live Updates）
这是 Android 对 iOS「实时活动」的官方对应物。本次**按语义分级接入**，而不是把所有通知都塞进去：

| 级别 | 场景 | 做法 |
|---|---|---|
| **Live Updates** | 余额跌破阈值 | `ProgressStyle` 进度条 + `setOngoing(true)` + 请求提升，可上灵动岛 / 锁屏 / 状态栏胶囊 |
| **普通通知** | 套餐临期、模型上新 | 标准高优先级通知，**不请求提升** |

- **为什么不全接**：Google 明确规定 Live Updates 只服务「用户此刻正在等待的进行中事项」，并警告不属于该范畴却请求提升会被**静默降权**。余额告警够格是因为它是个**有终点的持续状态**（从跌破阈值到充值回升或耗尽归零），用户确实在等结果；临期/上新是瞬时事件，硬套进度条属滥用。
- **进度语义**：跌破阈值 = 100%（刚开始危险），彻底耗尽 = 0%（进度条走完）。即「还剩多少安全余量」。
- **自动撤销**：余额回升到阈值 1.2 倍以上（充过值了）自动清掉常驻告警。
- **优雅降级**：Android 16 以下设备自动退化为普通常驻通知，不影响功能。

#### 2. 通知体系重构
- 新增 `notify/Notifier.kt` 作为**全项目唯一发通知入口**，内置分级策略文档
- 通知 ID 按「渠道 + 账户」生成，避免三类通知互相顶掉
- 新增通知栏专用单色图标 `ic_stat_balance.xml`

### 🔧 优化与修复

#### 3. 进入 App 不再立即刷新，改为纯定时
- **问题**：每次冷启动都会立刻打一轮接口。根因是「上次同步时间」存在内存变量里，**进程重启即归零** → 每次都误判"从没同步过"。
- **修复**：时间戳持久化落盘，跨进程重启、跨覆盖升级都继续走定时。
- 刷新周期收敛为**唯一来源常量**（15 分钟），前台定时器、后台 Worker、进页面判断全部引用它。
- 顶栏显示「更新于 HH:mm · N 分钟后刷新」倒计时。
- 手动「刷新」按钮仍可绕过周期立即执行。

| 场景 | 行为 |
|---|---|
| 距上次同步 < 15 分钟进 App | 不打接口，读上次数据 |
| 距上次同步 ≥ 15 分钟进 App | 自动刷一轮 |
| App 开着 | 每 15 分钟自动刷 |
| 点「刷新」 | 立即刷（绕过周期） |
| **卸载重装** | 数据清 → 时间戳归零 → 首次必刷 |
| **覆盖升级** | 数据保留 → 时间戳保留 → 仍受定时约束 |

#### 4. 修复异常 / 偏低余额反复通知的 bug
- **问题**：余额告警去重失效，每次进 App 都重复弹通知。
- **根因**：去重条件是 `if (lastWarned < 0 || num < lastWarned - 0.01)`，注释写「-1 = 从未告警」。但**余额可以是负数**（扣费超充值，实测 DeepSeek `¥-0.46`），负数同样满足 `lastWarned < 0` → 被当成"从未告警" → 每次刷新都重发。
  > 这是同一类坑第二次踩：之前是「余额 0 撞哨兵 0」，把哨兵从 0 改成 -1；这次是「负余额撞哨兵 -1」。**余额的取值范围是全体实数，任何数值哨兵都会被某个合法余额撞上。**
- **修复**：
  - 用**独立布尔标志** `notified_once_<id>` 表示"是否告警过"，不再用数值哨兵
  - 下跌幅度算法从乘法系数改为差值比 `relDrop = (last - num) / max(1, |last|, |num|) ≥ 0.2`（原写法 `num < last * 0.99` 对负数会反向）
  - 同步加 `Mutex` 串行化闸门 + UI 防重入标志，避免并发触发重复通知
- **验证**：修复前连续 4 次启动通知数 `0 → 1 → 3`；修复后 `0 → 0 → 0 → 0`。

#### 5. 各厂商卡片换用官方品牌图标
- DeepSeek / Kimi / Moonshot / OpenRouter / 智谱 使用各厂商**官方品牌标识**（Simple Icons，CC0）
- New API 为社区项目无官方条目，沿用自绘「双向箭头」（中转语义）
- **平台身份靠图标形状 + 名称区分，不靠色相**（此前试过"一平台一色"，等于在一套色阶里塞 5 个无关色相，已被推翻）

#### 6. 密钥存储升级
- 改为 **AES-256-GCM + Android Keystore**，密文格式 `enc1:<iv>:<ct>`
- 旧版明文存储自动迁移（`migrateOnce()`）

#### 7. 其他修复
- 智谱 WebView 内容区空白：`html` 计算高度塌成 0，内容被 `overflow:hidden` 裁掉。修复为**持续自校正**（resize / orientationchange / popstate / pushState / MutationObserver）
- 余额 ≤ 0 判「异常」：`parseBalance` 正则此前未捕获负号，`¥-0.46` 被解析成 `0.46`，永远轮不到负余额分支
- New API 余额读数修正：`quota` 本身就是剩余额度，不再误减 `used_quota`；通道优先级改为「先 `/api/user/self`（账户余额）再 billing（令牌额度上限）」；识别"无限额度"哨兵值 `100000000` 不当余额展示

### 📦 下载信息

- **版本号**：1.1.0
- **版本代码**：2
- **最低系统**：Android 8.0 (API 26)
- **目标系统**：Android 15 (API 35)
- **编译版本**：Android 16 (API 36，灵动岛需要)
- **构建工具**：AGP 8.9.1 / Gradle 8.14.4 / Kotlin 2.0.21

---

## v1.0.0 — 初次发布

> 首个正式版本，Kotlin + Jetpack Compose 开发。

### ✨ 新增功能

- **多厂商额度查询**：DeepSeek / New API 中转站 / 智谱 / Kimi / OpenRouter 统一管理
- **额度详情页**：余额大卡 + 套餐明细 + 模型定价 + 最近调用记录
- **余额阈值告警**：低于自定义阈值时发通知
- **桌面小组件**：常驻显示余额快照
- **后台自动刷新**：WorkManager 周期性同步
- **安全密钥存储**：保护 API Key

### 🎨 UI 体系

- 采用 **Monet 2026 配色**与 **iOS26 Liquid Glass** 液态玻璃设计语言
- 卡片原生支持滑动删除（含撤销）与长按拖拽排序

### 📦 下载信息

- **版本号**：1.0.0
- **版本代码**：1
- **最低系统**：Android 8.0 (API 26)

---

> 历史版本详情将在后续迭代中持续补充。
