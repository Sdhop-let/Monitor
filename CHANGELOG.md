# 更新日志 (CHANGELOG)

> 本文档记录 `API 额度监控` 从开发至今的所有变更、优化与新增功能。

---

## v1.0.0 — 初次发布

> 首个正式版本，采用 Kotlin + Jetpack Compose 开发，面向 OnePlus 15 深度适配（6.78 英寸 / 2772×1272 / 450PPI）。

### ✨ 新增功能

#### 1. API 额度查询首页
- 多厂商实例统一管理，首页以卡片形式展示每个实例的**作者 / 内容预览 / 运行状态 / 余额**等信息
- 卡片支持**点击进入详情页**、**左右滑动删除**（含撤销）、**长按拖拽排序**（拖动到列表边缘自动滚动）
- 品牌专属渐变头像 + 状态角标，余额使用 135° 对角渐变展示（正常为蓝色、透支为珊瑚粉）
- 下拉刷新 + 手动一键刷新能力

#### 2. 额度详情页
- 大幅余额数字卡片 + 「额度/套餐」明细区块
- 支持配额 / 套餐 / 消耗明细的多维度展示
- 顶部圆形返回按钮 + 刷新按钮，围绕居中的模型名称胶囊对称布局

#### 3. 添加 / 编辑实例
- 完整表单：显示名称、API Key、Base URL、User ID、自定义解析、告警阈值
- 厂商类型下拉选择，字段 `fillMaxWidth` 左右对齐
- 支持启用开关、额度检测、**12 小时自动刷新**

#### 4. 免费 AI 模型聚合页
- 国内 / 国际分区，免费模型卡片区分「注册即可用」与「直连可用」
- 数据来源：精选目录（优先）+ OpenRouter + SiliconFlow + 商汤 SenseNova + Google Gemini + Cohere + Cerebras + Together AI + OVHcloud

#### 5. 桌面小组件
- 支持多尺寸（含 4×2 紧凑竖直布局，优化字体避免重叠）
- 统一深蓝渐变主题，多厂商数据同步

#### 6. 系统能力
- **12 小时自动刷新** + 余额阈值告警通知（`BalanceAlertNotifier`）
- 前台刷新器 + 后台 Worker（`RefreshWorker` / `FreeModelRefreshWorker`），支持并行为多实例刷新
- 安全密钥存储（`SecureKeyStore`）保护 API Key
- WebView 登录页支持（新 API / 智谱 / 商汤）

### 🎨 UI 优化

- 全面采用 **Monet 2026 配色**（主色 `#4C6FB4`）与 **iOS26 Liquid Glass** 液态玻璃设计语言
- 背景使用校准的 `#EEF2FB`，顶部标题栏采用 Liquid Glass 效果
- 卡片透明度分级 `0.94 / 0.86 / 0.78`，保证内容清晰不干扰背景
- 底部胶囊导航栏：选中态 40% 透明蓝，未选中态 40% 透明灰
- 顶部标题贴合状态栏（最小 2dp 内边距），不遮挡系统图标
- 实例名称统一显示为「自定义名称·厂商名称」（如「小鲸鱼·DeepSeek」）

### 🔧 优化与修复

- **WAF 兼容性**：针对部分 API 服务器基于 TLS 指纹（JA3）拦截 OkHttp/Conscrypt 的问题，`ApiClient` 强制 HTTP/1.1 + 重试，并以 `WebViewHttp`（首页可见 WebView + 同源 fetch）作为回退方案，成功穿透拦截
- **并行刷新**：`refreshAll` 改为并行执行，单个挂起实例不再拖慢其余正常实例
- **撤销删除**：滑动删除采用"先不删数据、提示撤销"模型，点击撤销可平滑复位卡片，杜绝误删
- **长按拖拽**：回归稳定的 `detectDragGesturesAfterLongPress` API，修复自定义手势拦截点击/滑动的问题
- 交互优先层级：点击进详情 / 滑动删除 / 长按排序互不干扰
- 长按触发系统震动反馈，提升触感体验

### 📦 下载信息

- **版本号**：1.0.0
- **版本代码**：1
- **最低系统**：Android 6.0 (API 23)
- **目标系统**：Android 15 (API 35)

---

> 历史版本详情将在后续迭代中持续补充。